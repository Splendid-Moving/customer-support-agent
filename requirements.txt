# Runtime dependencies for deployment.
#
# pyproject.toml stays the source of truth for local development. This file
# exists because Railway's Nixpacks builder only auto-installs from a
# requirements.txt, Poetry, or PDM layout.
#
# Versions are PINNED, not ranged. With ranges, pip backtracks through a large
# number of langchain/langgraph combinations and the build appears to hang.
# These are the exact versions ops-agent runs in production.

langchain==1.3.14
langchain-core==1.5.3
langgraph==1.2.10
langsmith==0.10.17

langchain-openai==1.4.2

fastapi==0.141.1
uvicorn==0.52.1
python-multipart==0.0.32

langgraph-checkpoint-sqlite==3.1.1

requests==2.34.2
pydantic==2.13.4
python-dotenv==1.2.2
